import { a, b, c} from './a' // 批量导入
import { A } from './a' // 导入接口
import { d as D} from './a' // 导入时起别名
import * as All from './a' // 导入模块的所有成员，并且绑定在All变量上
import myFunction from './a' // 不加任何{}，使用默认导入


